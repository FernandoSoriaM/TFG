using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
[System.Serializable]
public class OutfitCharacter
{
    public float[] position;
    public float[] size;

    /*public OutfitCharacter (CharacterTransform character){
        position = new float[3];
        size = new float [3];
        position[0] = character.transform.position.x;
        position[1] = character.transform.position.y;
        position[2] = character.transform.position.z;

        size[0] = character.transform.localScale.x;
        size[1] = character.transform.localScale.y;
        size[2] = character.transform.localScale.z;
    }
}*/